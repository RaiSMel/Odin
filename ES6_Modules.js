// para criar um gerenciador de pacotes e salvar todas informações do projeto utilize o 
// > npm init

// Isso possibilita enviar todo os arquivos exceto por essas bibliotecas que podem ser baixadas posteriormente por
// > npm install

// var moment = require('moment');
// console.log("Hello from JavaScript!");  
// console.log(moment().startOf('day').fromNow());


// framework para utilizar na engine do node.js, esse é a mais utilizada, sendo ela usada pelo react
// >npm install webpack webpack-cli --save-dev
// > ./node_modules/.bin/webpack index.js --mode=development

// a diferença entre devdependencie e dependencie são respectivamente para plugins para construção do softare e para o software em producao / --save-dev --save

// documentação para o npm https://docs.npmjs.com/

// Para instalar os webpack use o npm i webpack


// Os modulos mudam apenas na adição de dois componentes o import e o export

// export é usado para exportar para outro arquivo as funções que vc criou

// import é utilizado para usar as funcoes que vc criou ou importou de um pacote, biblioteca, frameworks

// esses comandos são utilizados para códigos que são executados em diferentes partes dos códigos e possuem um tipo de padronização